<UserControl x:Class="WebSearcher.Views.Templates.SearchTermControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="74" d:DesignWidth="492">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="39*" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding ArticleTitle}" Margin="5" FontSize="18" FontWeight="Bold" />
        <TextBlock Grid.Row="1" Text="{Binding CalloutText}" Margin="5" Foreground="#FF585858" />
    </Grid>
</UserControl>
